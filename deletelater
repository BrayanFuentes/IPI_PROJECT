bool isPrime(int value)
{
	bool prime = false;
	if (value == 1)
	{
		return prime;
	}
	else
	{
		for (int i = 2; i < value; i++)
		{
			if (value % i == 0)
			{
				prime = false;
			}
			else
			{
				prime = true;
			}
		}
		return prime;
	}
	
};
